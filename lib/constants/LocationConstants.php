<?php

class LocationConstants {
  const TYPE_OUTSIDE = 1;
  const TYPE_BUILDING = 2;
  const TYPE_VEHICLE = 3;
  const TYPE_SUBFIELD = 4; // What's that? It's never used, but maybe it was supposed to be something like sub locs of central area
  const TYPE_SAILING_SHIP = 5;
  
}

?>